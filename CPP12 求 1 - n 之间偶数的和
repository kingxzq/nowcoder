#include <iostream>
using namespace std;

int main() {
	
	int n;
	cin >> n;
	int sum = 0;
    for(int i = 2; i <= n; i += 2) //从2开始，每两个元素遍历一次
        sum += i; //累加
	cout << sum << endl;
	return 0;
}
