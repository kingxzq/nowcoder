#include <iostream>
using namespace std;

int main() {

    int n;
    cin >> n;
    
    //新建数组
    int* p=new int[n];
    //给数组元素赋值
    for(int i=0;i<n;i++){
        p[i]=n+i;
    }
    
    //输出数组中的元素
    for(int i=0;i<n;i++){
        cout<<p[i]<<" ";
    }

    return 0;
}
