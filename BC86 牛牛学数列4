#include <stdio.h>

int main() {
    int n,i,j=0,ret=0;
    scanf("%d",&n);
    while(n)
    {
        i=n;
        while(i)
        {
            
            j+=i;
            i--;
        }
        ret+=j;
        j=0;
        n--;
    }
    printf("%d",ret);
    return 0;
}
