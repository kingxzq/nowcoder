#include <stdio.h>

int main(void)
{
    int m,n,temp;
    scanf("%d %d",&m,&n);
    int *p1 = &m;
    int *p2 = &n;
    //交换
    temp = *p1;
    *p1 = *p2;
    *p2 = temp;
    
    printf("%d %d\n",m,n);
    
    return 0;
}
