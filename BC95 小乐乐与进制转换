#include <iostream>
#include <math.h>
using namespace std;

int main() {
    long long n;
    while (cin >>n) {
        long long ret=0;
        for(int i=0;n;++i)
        {
            ret+=(n%6)*pow(10,i);
            n/=6;
        }
        cout<<ret<<endl; 
    }
}
