#include <stdio.h>
#include <math.h>
int main() {
    double a,b,c,p;
    scanf("%lf%lf%lf",&a,&b,&c);
    p=(a+b+c)/2.0;
    printf("circumference=%.2lf area=%.2lf",p*2.0,sqrt(p*(p-a)*(p-b)*(p-c)));
}
