#include <iostream>
#include <set>
#include <string>
using namespace std;

int main() {
    string str;
    while (cin >>str) { 
        set<char> s;
        for(int i=0;i<str.size();++i)
            s.insert(str[i]);
        cout<<s.size()<<endl;
    }
    return 0;
}
