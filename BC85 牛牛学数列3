#include <stdio.h>

int main() {
    double n,i=0;
    scanf("%lf",&n);
    while(n)
    {
        if((int)n%2==0)
        {
            i-=1/n;
        }
        else
        {
            i+=1/n;
        }
        n--;
    }
    printf("%.3lf",i);
    return 0;
}
