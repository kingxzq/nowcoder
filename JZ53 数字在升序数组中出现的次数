int GetNumberOfK(int* nums, int numsLen, int k ) {
    int ret=0;
    for(int i=0;i<numsLen;i++)
    {
        if(nums[i]==k)
            ret++;
        if(nums[i]>k)
            break;
    }
    return ret;
}
