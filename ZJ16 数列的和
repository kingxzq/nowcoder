#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a, b;
    while (cin >> a >> b) {
        double n=a;
        while(--b)
        {
            n=sqrt(n);
            a+=n;
        }
        printf("%.2lf\n",a);
    }
}
